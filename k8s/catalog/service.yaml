apiVersion: v1
kind: Service
metadata:
  name: catalog        # ← naam change karo
  namespace: ecommerce
spec:
  type: ClusterIP
  selector:
    app: catalog       # ← naam change karo
  ports:
    - port: 80
      targetPort: 3000