apiVersion: v1
kind: Service
metadata:
  name: cart        # ← naam change karo
  namespace: ecommerce
spec:
  type: ClusterIP
  selector:
    app: cart       # ← naam change karo
  ports:
    - port: 80
      targetPort: 3000