apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway
  namespace: ecommerce
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gateway
  template:
    metadata:
      labels:
        app: gateway
    spec:
      containers:
        - name: gateway
          image: 303713699681.dkr.ecr.us-east-1.amazonaws.com/eks-commerce-gateway:d59f0a1feb61
          ports:
            - containerPort: 3000